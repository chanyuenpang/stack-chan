# OTA + 真机 + 回退 验收记录模板

> 目的：统一记录 OTA 发布、真机关键链路验证、回退验证的完整验收证据。
> 原则：先记录事实，再给结论；可复现、可审计、可追溯。

---

## 元信息

- 记录编号: `ACC-YYYYMMDD-XXX`
- 记录时间: `YYYY-MM-DD HH:mm:ss +08:00`
- 执行人: ``
- 评审人: ``
- 环境: `staging / production / lab`

---

## 全局约束与停止边界

- 当前版本（Current Version）: ``
- 目标版本（Target Version）: ``
- 设备 ID: ``
- 设备型号/批次: ``
- 网络条件: ``

### 停止边界（任一命中即停止后续动作）

- [ ] 设备无法启动 / 连续重启（Boot Loop）
- [ ] OTA 关键步骤出现不可恢复错误（如签名校验失败且无法重试）
- [ ] 真机关键链路主功能失败且无法在 1 次重试内恢复
- [ ] 回退后版本不一致或关键功能仍失败
- [ ] 出现安全风险、数据破坏风险或不可接受副作用

---

## 场景 A：OTA 发布结果（Before / Action / After / Conclusion）

### Before

- 发布前版本: ``
- OTA 包信息（版本号/构建号/哈希）: ``
- 发布目标范围（设备/分组）: ``
- 前置检查结果（签名、大小、依赖）: ``
- 基线状态截图/日志路径: ``

### Action

- 执行命令:

```bash
# 示例：按项目实际命令填写
# ./ops/bin/xxx-ota-publish.sh --version <target> --device <device_id>
```

- 实际执行步骤（时间线）:
  1. ``
  2. ``
  3. ``

### After

- OTA 发布结果摘要: ``
- 关键日志/证据（路径或粘贴片段）:

```text
<日志片段>
```

- 结果判定（PASS/FAIL）: ``
- 判定依据: ``

### Conclusion

- 结论: `PASS / FAIL`
- 风险与备注: ``
- 是否进入下一场景（真机验证）: `Yes / No`

---

## 场景 B：真机关键链路验证（Before / Action / After / Conclusion）

### Before

- 验证设备 ID: ``
- 设备当前版本: ``
- 关键链路清单（至少 3 条）:
  - [ ] 链路 1: ``
  - [ ] 链路 2: ``
  - [ ] 链路 3: ``
- 前置状态检查: ``

### Action

- 执行命令:

```bash
# 示例：按项目实际命令填写
# ./ops/bin/xxx-device-check.sh --device <device_id>
```

- 手工操作步骤（如需）:
  1. ``
  2. ``

### After

- 每条链路结果:
  - 链路 1: `PASS/FAIL`，证据: ``
  - 链路 2: `PASS/FAIL`，证据: ``
  - 链路 3: `PASS/FAIL`，证据: ``
- 关键日志/证据:

```text
<日志片段>
```

- 整体结果判定（PASS/FAIL）: ``
- 判定依据: ``

### Conclusion

- 结论: `PASS / FAIL`
- 风险与备注: ``
- 是否进入下一场景（回退验证）: `Yes / No`

---

## 场景 C：回退验证（Before / Action / After / Conclusion）

### Before

- 回退前版本: ``
- 目标回退版本: ``
- 回退策略（整包/差分/安全回退）: ``
- 回退前风险确认: ``

### Action

- 执行命令:

```bash
# 示例：按项目实际命令填写
# ./ops/bin/xxx-ota-rollback.sh --device <device_id> --to <version>
```

- 实际操作步骤:
  1. ``
  2. ``
  3. ``

### After

- 版本校验结果（应回到目标回退版本）: ``
- 回退后关键链路复测结果: ``
- 关键日志/证据:

```text
<日志片段>
```

- 结果判定（PASS/FAIL）: ``
- 判定依据: ``

### Conclusion

- 结论: `PASS / FAIL`
- 风险与备注: ``
- 是否允许结束本次验收: `Yes / No`

---

## 总结（最终）

- 三段式总结果: `PASS / FAIL`
- 最终结论说明（必须可审计）: ``
- 后续动作:
  - [ ] 无
  - [ ] 需修复后重验
  - [ ] 需扩大验证范围
- 附件索引（日志、截图、录像、工单链接）:
  - ``
  - ``
