# tiny-world - Project Memory

| 标题 | 内容摘要 | 计数 |
|------|---------|------|
| 秘境系统核心设计决策 | Yop 对秘境系统的核心定义：1）临时入口交互物，复用现有框架；2）临时地图跳转，有明确退出条件（时间到/被击败/打完Boss/获得核心content）；3）每个秘境有一个核心content（核心掉落/核心剧情/核心机缘），获得即结束；4）地图是"固定骨架+变量填充"模式（固定据点/核心NPC/世界规则），不是纯随机生成，不是逐层爬塔（已明确抛弃逐层概念，秘境是单层临时空间）。架构关键：秘境是Map层面的临时替换，不是World切换也不新增SubScene。当前World保持不变，临时缓存大世界Map→加载秘境Map（MapType.DUNGEON）→秘境结束恢复原Map。战斗仍走现有COMBAT场景。秘境入口有时限，使用岁月系统的游戏内时间，默认3年，到期后入口消失/秘境强制结束。秘境设计的四个核心维度：1）难度（怪物强度、境界缩放）；2）游戏规则（时限、特殊机制、世界规则）；3）地图设计（固定骨架、变量填充、探索路径）；4）奖励（核心content+通关奖励+首通奖励）。 | 1 |
| 秘境系统 MVP 交付状态 | 秘境系统 MVP 代码全部完成，Godot 编译零错误（10个脚本全部通过）。SkillManager/SpellManager autoload 已修复。全面检查完成：7个问题已修复，MCP测试10/10全部通过，落地效果已验证。 | 1 |
| 存档系统需适配 GameMode 切换 | 项目支持 GameMode 随时切换（如 mortal_realm_early），存档系统需要适配不同的 gamemode 设置，确保切换/加载时正确恢复对应的 GameMode 状态。 | 1 |
| MCP Ingame 实机测试路径调研 | 6个游戏系统的实机MCP测试链路已调研完成： - 战斗: list_interactables → teleport → trigger_interact/run_script(force_interact) → 验证场景切换 - 定居点: teleport → run_script(entrance._trigger_interaction()) → 验证面板打开 - 机缘/命运: get_fate_graph_info → force_unlock_content → run_script(report_event) → 验证状态 - NPC交互: teleport(NPC 48px内) → run_script(zone._open_menu()) → 选择选项 - 灵草采集: list_interactables → teleport → trigger_interact → 验证Clue（已验证通过） - 副本/秘境: teleport → run_script(enter_dungeon) → 验证Map切换 关键发现：多数系统不能用 trigger_interact 直接触发，需要 run_script 调内部方法绕过碰撞 | 1 |
| MCP 测试脚本禁止 await | MCP run_script 同步调用 execute()，不支持 await。脚本中任何 await 都会导致返回值变成协程状态对象（表现为 null）。所有测试脚本必须使用同步逻辑，如果必须等待物理帧则返回 skipped:true + skip_reason。 | 1 |
| CombatEventContainer 继承 RefCounted 非 Node | CombatEventContainer 继承链是 CombatEventContainer → BaseContainer → RefCounted，不是 Node。不能 add_child 到场景树，不能 queue_free()。new() 会自动调用 _init()，不要重复调用。 | 1 |
| Facility NPC 匹配语义决策 | 用户确认：facility NPC 不需要 settlement 过滤。对于同一个 facility ID，只要 facility 匹配即可领取；不同 settlement 的同名 facility 应通过 facilityID 本身使用 settlementID + facilityID 表达。如果 Content 配置了 facilityID，则允许部分匹配领取。后续不要再引入 ClaimFilter.params.settlement_id 方案。 | 1 |
| Facility NPC 认领规则与持久化策略设计 | 用户确认的 facility NPC 认领规则（2026-05-06）： 1. 认领规则不变：只指定 facility 时，按部分匹配原则，谁先触发认领就属于谁。不指定 settlement 时 NPC 可能被任意匹配 facility 认领，这是正确行为。 2. 固定 NPC（如客栈老板娘）使用 temporary 持久化策略： - 直接配置到设施的 RoleContainer 中（role_ids 或 components_config） - temporary 类型 NPC 初始化时允许复制：每个同类设施都会有自己的固定 NPC - 复制后 ID 格式：settlementID_facilityID_NPCID - 每个客栈都会有自己的老板娘 3. temporary 类型 content 仍然遵循统一的认领规则（谁先触发谁认领），包括默认解锁内容。 4. 认领触发时机： - 只有 Map 在进入游戏时触发默认认领 - 设施/据点的 container 在其获取内容时才触发 claim - 不是在创建 container 时触发 claim 设计方向：不改 ClaimFilter settlement 语义；facilityID 部分匹配保持；固定 NPC 通过 temporary + RoleContainer 配置解决。 | 1 |
| tiny-world Harvest DataTable cleanup and MCP verification | 2026-05-06：MenuAction 采摘掉落 DataTable 化清理已提交并推送，commit d1b72e60708385615efa538a45c610580d1b5b0b。之后补做 MCP 实际游戏流程验证 PASS：真实场景 spirit_grass_002 通过 force_interact 触发 MenuAction -> harvest_action.tres -> Blueprint -> HarvestHerbNode；DataTable drop_asset_path %d 解析为 spirit_grass_1year.tres；delivery_mode=inventory 背包数量 0->1；consume_target=true 后目标移除；SCRIPT ERROR 和 HarvestHerbNode 过滤错误均为 0。 | 1 |
| tiny-world 通用交互菜单规则 | 用户明确基础规则：如果交互物只有一个交互行为，玩家靠近时提示显示为 `E [交互菜单文字]`，按 E 后直接触发该菜单；如果交互物有多个交互行为，玩家靠近时提示显示为 `E 交互`，按 E 后弹出交互菜单。该规则是通用规则，需审视并统一所有交互物：NPC、灵草、传送门、秘境入口、据点入口、宝箱等。 | 1 |
| 客栈设施面板 RoleContainer NPC 修复 | 2026-05-06 修复 tiny-world 客栈设施面板看不到配置 NPC 的问题。根因：打开设施面板时 facility_entity 未稳定传入，面板无法稳定借用运行时设施实体上的 RoleContainer。修复 SettlementManager.gd 增加从 settlement entrance meta 兜底获取 settlement_entity；FacilityPanel.gd 增加从 settlement_entity.FacilityContainer 反查 facility_entity，并仅在运行时 RoleContainer contents 为空且 role_ids 非空时兜底 load+spawn_temporary_roles，避免清掉 temporary NPC。独立验证通过：inn_facility RoleContainer role_ids 包含 npc_mortal_realm_innkeeper，面板 fallback 找到运行时实体，panel_role_size=1，has_innkeeper=true，has_temporary_copy=true。Commit 71f0f75d 已 push main。 | 1 |
| tiny-world 宝箱打开掉落规则 | 用户明确：宝箱按 E 打开时，也应该生成地图 Pickup，流程要和采摘完全一样；不能直接把奖励放入背包。后续测试应验证：E 打开 -> 生成 Pickup -> 玩家拾取 -> 背包增加 -> Pickup 消失。 | 1 |
| tiny-world 全局交互规则与 Pickup 奖励提交 | 2026-05-06：全局交互规则统一与 Pickup 奖励流程已完成并推送。Commit 62456a0ab5a87f55f39cb8279e0ffba6707796cb / feat(interactable): unify interaction prompts and pickup rewards。规则：单行为显示 E [菜单文字] 并真实 E 直触发；多行为显示 E 交互并弹菜单。宝箱打开改为生成地图 Pickup，不再直接入包；MCP 验证 PASS：E 打开后背包不立即增加，生成 spirit_grass_2year Pickup，拾取后背包 +1 且 Pickup 消失。提交还包含 OpenChestNode、全局交互 HUD/输入规则、传送门/秘境 MCP fixture、全覆盖复测文档。 | 1 |
| tiny-world Interaction hint display fix | 2026-05-07：修复靠近交互物不显示 E 键提示。GameHUD.gd 改为按实体所属 GameViewport/SubViewport 坐标定位交互提示，并在 _ready 提前加入 game_hud group。MCP 验证 PASS：灵草 E 采摘、宝箱 E 打开、NPC E 交互均可见且在 1920x1080 视口内，真实 E 行为正确。提交 ee2a1a67f0d20628a719ba6e8153e8e4f7b15f7a 已 push origin/main。 | 1 |
| tiny-world Settlement entrance hint fix | 2026-05-07：修复据点入口不显示 E 进入提示。SettlementEntrance 改为优先使用统一 GameHUD.add_interaction_hint/remove_interaction_hint，保留本地 InteractionHint 作为 fallback。MCP 验证 PASS：靠近据点 HUD 显示 E 进入且在视口内，真实 E 打开 SettlementPanel，灵草/宝箱/NPC 提示抽查无回退，日志门禁 0。Commit da982573c34287c478d54979d35e31f5afe0f736 已 push origin/main。 | 1 |
| tiny-world Battle and Dungeon interaction hints fix | 2026-05-07：继续排查其他交互物提示，修复 BattleEventInteractable 与 DungeonEntrance。BattleEventInteractable 新增运行态检测、GameHUD 提示 E 战斗、真实 E 输入链路和初始重叠扫描；DungeonEntrance 接入统一 GameHUD 显示 E 进入秘境。MCP 复测 PASS：Battle state INTERACTABLE -> 真实 E 后 INTERACTING，Dungeon/Teleport/宝箱/灵草/据点提示可见，日志门禁 0。Commit ce8663f9 已 push origin/main。 | 1 |
| 交互物统一框架改造完成 | tiny-world generic-interactable：已完成并推送秘境入口与据点入口统一到 InteractableObject 通用框架。设计文档 commit 64a468be；功能提交 f43198ce（feat: 统一秘境和据点入口交互框架）已 push 到 main。新增 DungeonInteractable/SettlementInteractable、对应 .tres/.tscn 模板；适配 Dungeon fixture、ChunkManager、SettlementManager、ExplorationScene、4 个据点场景；legacy settlement_entrance.tscn 已改为 SettlementInteractable；Godot/MCP 验证通过。仍有未提交非本任务文件 .claw/truth/features/dungeon-interactable-entrance.md。 | 1 |
| Truth 文档随任务一并提交 | 用户明确要求：涉及 .claw/truth / truth 文档的任务记录或状态同步，不要留到最后单独收尾；应与对应代码/文档任务一并纳入提交，保证工作区干净且提交链完整。 | 1 |
| 角色认领唯一性规则 | tiny-world 的角色/content 认领规则：如果角色是 unique，则同一个角色/content 一旦被任意容器认领，就不应被其他容器再次认领；如果是 temp 类型，则允许被多次认领。该规则用于修复钓鱼老人已在地图认领后又在客栈内被重复认领的问题。 | 1 |
| unique/temp 属于 GameEntityConfig 而非 ContentConfig | tiny-world 角色唯一性规则：unique/temp 是 GameEntityConfig/GameEntityAsset 自带字段，不应在机缘 ContentConfig 中重复配置 claim_mode。认领/创建角色时应通过 content.asset_reference 指向的 GameEntity 配置读取 unique/temp，并基于当前世界实体事实检查是否已存在同一 unique 角色；temp 允许重复。 | 1 |
