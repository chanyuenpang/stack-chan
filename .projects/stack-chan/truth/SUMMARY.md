# StackChan Truth Summary

| 文档 | 主题 | 可检索摘要 |
| ---- | ---- | ---- |
| `adr/defer-dev-local-http-until-xiaozhi-ready.md` | ADR: Dev Local HTTP 延后到 XiaoZhi ready 后启动 | 本地 Dev HTTP server 不得只等 STA IP 就启动；必须一次性创建 deferred task，并在 STA IP 与 `hal_bridge::is_xiaozhi_ready()` 均满足后等待 5000ms cooldown、二次确认再启动；60s 未 ready 则放弃启动，以避免 HTTP 控制面与 XiaoZhi/App 初始化抢资源。`PROJECT_VER` 保持 `1.4.1`，不通过改版本号修复崩溃。 |
| `adr/stackchan-remote-celebration-control-boundaries.md` | ADR: StackChan 远控庆祝控制边界 | 远控庆祝链路以设备端本地 Dev HTTP、MCP tool 分发、非阻塞运动调度、音频注入和 OTA rollback 链路为架构边界；外部系统只编排 HTTP/API 调用顺序。当前权威文档入口为 `docs/research/stackchan-remote-celebration-runbook.md`；OTA 快捷脚本使用纯数字 `VERSION` 与 `--force 1`；同版本 OTA 应通过 manifest `force=1` 和用户手动 `Check for Updates` 触发，不为升级临时修改源码版本号；点击 App 崩溃类回归以 `v2.0.29` 已验证 HTTP endpoint 清单作为验收基线。 |
| `PROJECT-TRUTH.md` | 项目级 truth 约定与索引 | canonical truth 归档规则；firmware dev 控制面需要确认编译开关、真实入口、调用链路与安全边界；OpenClaw plan 完成触发庆祝时不要误用未接线的 `planEventListeners` 或 `subagent_ended`；OTA 调试必须核对版本、manifest、bin 路径、SHA256/size 与 `force` 口径，并把 `Firmware upgrade successful` / reboot 前闭环与 reboot 后目标版本确认分开验收；`self.robot.celebrate` 的真实庆祝链路应保持 active，不用 no-op/fallback 掩盖风险，启动与 scheduler release/stop 阶段都必须 no-read。 |
| `features/boot-mode-xiaozhi-autostart-and-sys-evt-overflow.md` | Boot mode / XiaoZhi autostart 与 `sys_evt` 栈溢出 | `CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=4096`、`sys_evt stack overflow`、`boot/start_once`、`boot/fail_count`、`get_boot_mode_xiaozhi_auto_open_source()`、`set_boot_autostart_once_for_ota()`、`requestAutoOpenAiAgent()`、Launcher autonomous OTA 与 XiaoZhi `Application::Initialize()->board.StartNetwork()` 共享 Wi-Fi event 风险；2.0.31 现场链路是 `Boot 2.0.31 → Launcher → LAUNCHER-OTA task → startNetwork() → WiFi connecting → sys_evt STACK OVERFLOW → reboot`，未进入 `AI.AGENT` / XiaoZhi；2.0.32 OTA 后 Launcher idle、`SERVO-MOVE` / `SystemInfo` 持续、Wi-Fi stable 且无 `sys_evt` 崩溃可作为修复验收口径；说明 `start_once` 不受 fail_count 保护，OTA 成功后强制下次进 XiaoZhi 会把设备带入危险路径。 |
| `features/http-mcp-tool-entrypoints.md` | 本地 LAN dev HTTP 与官方小智 HTTP Messaging API 调用 MCP tools | `/dev/mcp/call`、`/api/messaging/device/tools/list`、`/api/messaging/device/tools/call`、`stackchan_mcp_dispatch_tool()`、`McpServer::ParseMessage()`、`McpServer::DoToolCall()`、`messaging token`、`X-StackChan-Dev-Token`；说明两条 HTTP 调用 MCP tool 路径的请求格式、鉴权、工具范围和安全边界。 |
| `features/nonblocking-head-motion-control.md` | 非阻塞头部电机 / 舵机控制链路 | `self.robot.set_head_angles`、`self.robot.set_head_targets`、`self.robot.celebrate`、`_stackchan_update_task`、`HeadMotionSchedulerModifier`、`moveWithSpeedNoHardwareRead`、`update_angle_anim_target(angle, false)`、`Servo::stopAnimation`、`abortAnimationNoRead`、`_angle_anim.directValue()`、`SERVO-DIAG source=stopAnimation read_hardware=0`、`SERVO-SCHED bus_healthy=0|1`、`Motion::stop`、`EnableTorque(id, 0)`、`ScsServo::set_angle_impl`、`stage=flush`、`stage=quiet`、`stage=ping`、`stage=read_pos`、`event=recovered`、bus dead、少读少写、final target once、scheduler release 后禁止硬件 `ReadPos`。 |
| `features/openclaw-plan-completion-hooks.md` | OpenClaw plan completion hooks for StackChan celebration | StackChan 远控庆祝链路应短期通过插件 `after_tool_call` 过滤成功的 `plan_edit` / `plan_write` 并读取落盘 plan 判断 `task done` / `plan completed`；`planEventListeners` 当前未在真实工具装配处接线；`subagent_ended` 不能代表 plan task done。 |
| `features/readpos-lvgl-lock-servo-bus.md` | `ReadPos` / LVGL lock / servo bus 黑屏卡死链路 | `ReadPos`、`ScsServo::getCurrentAngle`、`ScsServo::set_angle_impl`、`SCSCL::ReadPos`、`SCS::Read`、`SCS.h`、`SCS.cpp`、`SERVO-IO`、`50ms`、`checkHead`、`SCSerial::readSCS`、`IOTimeOut=10ms`、`uart_wait_tx_done`、`LvglLockGuard`、`lvgl_port_lock`、`_stackchan_update_task`、`motion.update()`、`GetStackChan().update()`、`StackChan::update()`、`_motion->update()`、`_update_in_progress`、`self.robot.get_head_angles`、`probe_bus_recovery`、`bus_dead`、`stage=flush/quiet/ping/read_pos`、`ServoBusManager`；说明异常 `ReadPos` 在持有 LVGL 锁时可阻塞约 `100-280ms` 导致渲染停顿，双 update、MCP/httpd 并发和共享 `_scs_bus` 会放大黑屏/卡死风险；第一阶段串行化已落在 `SCS` 基类协议层，用全局 FreeRTOS mutex 串行化所有 UART 事务、`50ms` 超时和 owner task 递归安全避免死锁；后续根治仍包括 UI 锁隔离、ReadPos 硬超时和 motion update 去重。 |
| `features/mooncake-launcher-capability-map.md` | MoonCake Launcher apps、设置页 OTA 与 XiaoZhi 阶段能力地图 | `app_main()`、`moon.installApp`、`AppLauncher`、`SETUP`、`SystemUpdateWorker`、`AI.AGENT`、`AppAiAgent::onOpen()`、`Hal::startXiaozhi()`、`start_dev_local_control_server()`、`start_dev_serial_wake_stop_task()`、`xiaozhi_mcp_init()`；说明 MoonCake apps 在 Launcher 阶段已注册，`SETUP` 设置页可手动触发 OTA，`AI.AGENT` 是进入 XiaoZhi runtime 的边界，HTTP 18080、MCP/reminder/celebrate 属于 startXiaozhi 后能力，dev serial 当前 no-op，纯 Launcher 远程 OTA 触发仍缺公开入口。 |
| `features/ota-firmware-update-chain.md` | OTA URL、manifest、本地固件更新、Launcher 前置触发、AP Web 写入、USB/NVS 修补 `ota_url` 与产物一致性链路 | `mooncake Launcher`、`AI Agent`、`AppAiAgent::onOpen()`、`requestXiaozhiStart()`、`startXiaozhi()`、`ActivationTask::CheckNewVersion()`、`ota_->CheckVersion()`、`Ota: Current version`、`Ota: New version available`、`Writing to partition ota_1`、`Progress: 100%`、`Firmware upgrade successful`、`Application: Firmware upgrade successful, rebooting`、`Project name`、`App version`、`BOOT-MODE`、`start_dev_serial_wake_stop_task()`、`STACKCHAN_ENABLE_DEV_LOCAL_HTTP_CONTROL`、`wifi.ota_url`、`wifi/ota_url`、`str_len=0`、`CONFIG_OTA_URL`、`https://api.tenclass.net/xiaozhi/ota/`、`firmware.version`、`firmware.url`、`firmware.force`、`force: 1`、`POST /ota/`、`HEAD /stack-chan.bin`、`GET /stack-chan.bin`、`Check for Updates`、`POST /advanced/submit`、`POST /submit`、`POST /exit`、`501 Not Implemented`、`501 Method Not Implemented`（esp_http_server 特有文本）、`HTTPD_501_METHOD_NOT_IMPLEMENTED`、`parser->method < 0`、`WifiConfigurationAp::StartWebServer()`、`httpd_register_uri_handler`、`httpd_uri_match_wildcard`、`max_uri_handlers = 24`、20 handler 注册表、`/generate_204*`、`ua=m5stack-stack-chan/1.4.1`、`app_version=`、managed component 无 git 历史、组件版本错位、`PROJECT_VER`、`FIRMWARE_VERSION`、`start-upgrade.sh`、`firmware/build/stack-chan.bin`、`exp-pkg/active-release`、`active.json`、`GET /ota/`、`firmware.force`、`force=0`、`force=1`、`project_description.json`、`sha256sum`、`strings <bin>`、`tools/ota-mock-server/ota-mock-server.py`、`patch_nvs_ota_url.py`、`nvs-patched-v2.bin`、`nvs-final.bin`、`esptool read_flash 0x9000 0x4000`、`esptool write_flash 0x9000 <nvs.bin>`、`ACTIVE page`、`Page 3`、`header entry`、`data entry`、`bitmap`、`page CRC`、`entry CRC`、`data CRC`、`ERASED`、`INVALID`、`ESP_OTA_IMG_PENDING_VERIFY`；说明本地 OTA server、强制更新、AP Web `/advanced/submit` 写入链路、所有 POST 返回 501 时不能确认 `ota_url` 已写入、esp_http_server 501 源码级鉴别、NVS 保留、真实设备 OTA 前半段闭环与 reboot 后目标版本确认要分开验收、无 `POST /ota/` 时应判定设备未触发/未到达 OTA 检查而非升级失败、不要覆盖 `activation` / `mqtt` / `websocket`、rollback 风险，以及重打包时版本/产物/manifest 错位排查。 |
| `features/launcher-to-xiaozhi-startup-chain.md` | Launcher 到 XiaoZhi 启动链路与自动启动可行性 | `AppLauncher`、`AppAiAgent`、`AppAiAgent::onOpen()`、`requestXiaozhiStart()`、`isXiaozhiStartRequested()`、`startXiaozhi()`、`start_xiaozhi_app()`、`openApp(appID)`、`requestAutoOpenAiAgent()`、`try_auto_open_ai_agent()`、`get_boot_mode_xiaozhi_auto_open_source()`、`boot/default_mode`、`boot/start_once`、`boot/fail_count`、`AppLauncherBase::onRunning()`、`AppLauncherBase::onSleeping()`、`LauncherView`、`warm_boot/app_index`、`start_dev_serial_wake_stop_task()`、`start_dev_local_control_server()`、`hal_bridge`、`mooncake`、`GetMooncake().update()`；当前固件已有 NVS boot mode 驱动的 Launcher auto-open `AI.AGENT` 机制，但 USB/HTTP/MCP 仍不能在纯 Launcher 阶段替代触摸或 boot mode；`start_once` 不受 `fail_count` 保护，`default_mode=xiaozhi` 才有 fail_count 阈值，后续改进应优先修 auto-start 救援边界。 |
| `features/launcher-no-network-recovery-paths.md` | Launcher default_off / `launcher_only` 无网络停留态与 UI 脱困路径 | `AI.AGENT`、`AppAiAgent::onOpen()`、`requestXiaozhiStart()`、`Hal::startXiaozhi()`、`Application::Initialize()`、`board.StartNetwork()`、`ActivationTask()`、`CheckNewVersion()`、`UpgradeFirmware()`、`SETUP`、`Firmware`、`Check for Updates`、`SystemUpdateWorker`、`GetHAL().startNetwork`、`Hal::updateFirmwareEx()`、`LAUNCHER-OTA disabled reason=default_off route=launcher_only action=stay_launcher`、`launcher_only`、`SERVO-MOVE`、`SystemInfo`、`WiFi scanning`、`CHECKING_NEW_VERSION`、`start_dev_serial_wake_stop_task()`、`STACKCHAN_ENABLE_DEV_SERIAL_WAKE_STOP`、`STACKCHAN_ENABLE_DEV_LOCAL_HTTP_CONTROL`、`start_dev_local_control_server()`、`WifiBoard::StartNetwork()`、`TryWifiConnect()`；说明 2.0.32 / 2.0.33 停在 Launcher 且无网络可属设计停留态，非 USB 脱困主要依赖触摸打开 `AI.AGENT` 或 `SETUP -> Firmware -> Check for Updates`，串口/HTTP/MCP/BLE/AP 在纯 Launcher 无网阶段不可作为可靠入口。 |
| `features/stackchan-celebrate-power-servo-stability.md` | `self.robot.celebrate` 庆祝动作的电源 / 舵机 / UART 稳定性 | `self.robot.celebrate`、`/dev/celebrate`、`start_celebrate_modifier()`、`2.0.26 no-op life-preserver`、`2.0.27-celebrate-stop-readfix`、`action=led_and_servo`、`action=scheduler_led_servo`、`source=celebrate_frame`、`moveWithSpeedNoHardwareRead`、`update_angle_anim_target(angle, false)`、`stackchan_celebrate_tick()`、`finishCelebrateLocked`、`Servo::stopAnimation`、`abortAnimationNoRead`、`_angle_anim.directValue()`、`SERVO-DIAG source=stopAnimation read_hardware=0`、`SERVO-DIAG source=abortAnimationNoRead read_hardware=0`、`SERVO-SCHED bus_healthy=0|1`、`Motion::stop`、`EnableTorque(id, 0)`、满亮 LED、两轴 final-target、`ScsServo::set_angle_impl()`、`WritePos`、`Ack(ID)`、`uart_wait_tx_done`、`stage=flush`、`stage=quiet`、`stage=ping`、`stage=read_pos`、`event=recovered`、`ESP_RST_BROWNOUT`、`SERVO-BUS recovery_probe_failed`、`reason=update_tick`、`raw=-1`、`bus_failed`、`prevent_disconnect`；说明庆祝硬死更像 LED/舵机/UART 峰值组合，HTTP/ping/USB/heap 稳定不代表伺服总线健康；no-op 只是保命，恢复真实庆祝必须同时避免启动阶段硬件读和 scheduler release/stop 阶段 `ReadPos`，但保留正常运动 auto sync。 |
| `features/stackchan-dev-local-http-celebrate.md` | 本地 HTTP 控制、XiaoZhi 音频注入、庆祝动作链路 | `STACKCHAN_ENABLE_DEV_LOCAL_HTTP_CONTROL`、`/dev/inject_prompt`、`AudioService::InjectPcmFrameToSendQueue`、`self.robot.celebrate`、`start_celebrate_modifier()`、`stackchan_celebrate_tick()`；说明 `/dev/wake`/`/dev/stop`、`/dev/celebrate`、`/dev/mcp/call`、`sound` 参数无效、MCP bridge 只读等长期约束。 |
| `features/stackchan-update-stability-window.md` | StackChan App 进入阶段更新稳定窗口 | `_stackchan_update_task`、`motion.update()`、`stackchan_celebrate_tick()`、`GetStackChan().update()`、`StackChan::update()`、`_motion->update()`、`_update_in_progress`、`stackchan_motion_high_refresh_active()`、`motion_update_interval_ms`、`vTaskDelay`、`LvglLockGuard`、`DisplayLockGuard`、`lvgl_port_lock`、`StackChanDisplay::SetStatus`、`request_stackchan_performance_mode()`；说明 2.0.5 保守 150ms 非 idle 更新窗口、50ms wake / active motion tick 回归风险、50ms motion update 与 150ms full update 双路径导致重复 `Servo::update()` 的风险，以及 Dev HTTP 非唯一根因。 |
| `features/launcher-autonomous-ota-check.md` | Launcher 自主 OTA 检查机制 | `start_autonomous_ota_check_once`、`updateFirmwareEx`、`OtaRunningGuard`、`s_ota_running`、`set_boot_autostart_once_for_ota`、`boot/default_mode`、`boot/start_once`、`boot/fail_count`、`LAUNCHER-OTA`、`LAUNCHER-OTA disabled reason=default_off route=launcher_only action=stay_launcher`、`BOOT-MODE set_next_boot`、`sys_evt`、`CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE`、`Ota: Current version`、`Ota: New version available`、`Writing to partition ota_1`、`Firmware upgrade successful`、`Application: Firmware upgrade successful, rebooting`、`2.0.32`、`Launcher idle`、`SERVO-MOVE`、`SystemInfo`、`HTTP 18080`、`Project name`、`App version`、`FirmwareUpdateResult`、`hal_ota.cpp`、`app_launcher.cpp`；说明 Launcher autonomous OTA 需先判断启用 / default_off 版本差异：启用时 view 创建后一次后台 OTA check，成功写 `default_mode=xiaozhi + start_once + fail_count=0` 后 reboot；default_off / `launcher_only` 时不会自动联网或访问 OTA server。自主 OTA 网络初始化也可能触发 `sys_evt` 栈溢出；default_off 下 Launcher idle 无 HTTP 18080 / 无 `/ota/` 请求是设计停留态。 |
