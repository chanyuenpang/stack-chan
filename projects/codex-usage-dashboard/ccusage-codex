#!/usr/bin/env bash
set -euo pipefail

args=()
skip_next=0

for arg in "$@"; do
    if [[ "$skip_next" -eq 1 ]]; then
        skip_next=0
    elif [[ "$arg" == "--locale" ]]; then
        skip_next=1
    else
        args+=("$arg")
    fi
done

exec ccusage codex "${args[@]}"
