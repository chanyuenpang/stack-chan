fixtureId: study-card-assistant
fixtureVersion: 0.1.0
kind: validation-result
profile: simple
validation:
  mode: static
  status: pending
  executed: false
  modelReplayExecuted: false
  summary: 静态验证入口尚未执行；本文件不声明真实模型回放已通过。
  checks:
    structure:
      status: pending
      note: 待校验 8 个 fixture 文件是否齐全，且 simple profile 仅包含单个 skill/SKILL.md。
    trigger:
      status: pending
      note: 待校验触发语义是否为学习材料整理成问答学习卡片，且非会议类。
    boundary:
      status: pending
      note: 待校验权限边界是否默认保守，不联网、不读本地、不写文件、不外发。
    dependency:
      status: pending
      note: 待校验是否无外部依赖或外部服务调用。
    replay:
      status: pending
      note: 待校验 replay cases 是否未伪造 observed/passed，且 modelReplayExecuted 为 false。
    privacy:
      status: pending
      note: 待校验是否不含真实学生、学校、账号、私有链接、访问令牌 或路径。
    compatibility:
      status: pending
      note: 待校验 YAML 与 Markdown 是否可被静态 MVP 读取；runtime、跨平台、跨模型均 pending。
pendingCapabilities:
  runtimeReplay: pending
  crossPlatform: pending
  crossModel: pending
permissions:
  network: false
  externalSend: false
  fileRead: false
  fileWrite: false
  destructiveOperations: false
  privatePathRead: false
dependencies:
  noneDeclared: true
  items: []
checklist:
  structure: validation-result 声明静态验证状态和 simple 结构要求。
  trigger: 包含非会议类 trigger 检查项。
  boundary: 包含 boundary 检查项与保守权限。
  dependency: 包含 dependency 检查项。
  replay: 明确未执行真实模型回放，modelReplayExecuted 为 false。
  privacy: 包含 privacy 检查项。
  compatibility: 包含 compatibility 检查项与 pending 能力说明。
