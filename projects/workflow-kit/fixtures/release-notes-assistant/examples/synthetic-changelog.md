# Synthetic Changelog Example

This file is fully fictional and synthetic. It does not describe a real repository, customer, issue tracker, internal host, private path, token, or production service.

## v0.4.0 Synthetic Demo Release

### Added

- Added batch export for sample reports in a demo workspace.
- Added compact view for a fictional task list.
- Added clearer empty-state copy for first-time demo users.

### Changed

- Renamed the synthetic workspace label from `workspace` to `project` in user-facing copy.
- Improved dashboard loading behavior for large generated demo datasets.

### Fixed

- Fixed duplicated labels in generated sample charts.
- Fixed a fictional settings panel message that used outdated terminology.

### Breaking Changes

- The synthetic config key `display_mode` is replaced by `view_mode` in example snippets.

### Migration Notes

- Review demo snippets that mention `display_mode` and update them to `view_mode`.
- Regenerate sample exports created before the fictional `0.3.0` demo version if they rely on compact view metadata.

### Known Limitations

- Compact view does not support archived demo items yet.
- Batch export examples only cover synthetic report data.

### Suggested Upgrade Checklist

- [ ] Confirm release notes are based only on public, fictional, or synthetic material.
- [ ] Review breaking changes and migration notes with a human maintainer.
- [ ] Avoid including private repository links, real issue numbers, real customer names, credentials, internal hosts/IPs, or private paths.
